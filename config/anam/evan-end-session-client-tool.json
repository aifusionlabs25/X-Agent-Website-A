{
  "name": "end_mullins_session",
  "description": "Arm Evan's confirmed-farewell close handshake after unmistakable closing intent. The visitor's goodbye, bye, take care, I'm all set, that's all, nothing else, or explicit request to finish is already confirmation. Call silently and never ask whether to end. The client returns farewell_required, keeps the audio stream open for exactly one brief warm farewell, and closes only after that farewell finishes.",
  "type": "CLIENT",
  "disableInterruptions": true,
  "config": {
    "parameters": {
      "type": "object",
      "properties": {},
      "required": [],
      "additionalProperties": false
    },
    "awaitResult": true,
    "toolTimeoutSeconds": 15
  }
}
